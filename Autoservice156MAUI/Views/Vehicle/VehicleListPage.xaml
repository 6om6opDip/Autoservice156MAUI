<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Autoservice156MAUI.Views.Vehicle.VehicleListPage"
             Title="Транспорт">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Добавить" Clicked="OnAddClicked" />
    </ContentPage.ToolbarItems>

    <ScrollView>
        <VerticalStackLayout Spacing="20" Padding="20">

            <Label Text="🚗 Транспортные средства" 
                   FontSize="24"
                   FontAttributes="Bold"
                   TextColor="#333333"/>

            <CollectionView x:Name="VehiclesCollection"
                            SelectionMode="Single"
                            SelectionChanged="OnVehicleSelected">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Padding="15" 
                               Margin="0,0,0,10"
                               BorderColor="#E0E0E0"
                               CornerRadius="10">
                            <VerticalStackLayout Spacing="8">
                                <Label Text="{Binding FullName}"
                                       FontSize="16"
                                       FontAttributes="Bold"
                                       TextColor="#333333"/>

                                <HorizontalStackLayout Spacing="15">
                                    <Label Text="{Binding LicensePlate}"
                                           FontSize="13"
                                           TextColor="#512BD4"/>
                                    <Label Text="•"
                                           FontSize="13"
                                           TextColor="#999999"/>
                                    <Label Text="{Binding Year, StringFormat='Год: {0}'}"
                                           FontSize="13"
                                           TextColor="#666666"/>
                                </HorizontalStackLayout>

                                <Label Text="{Binding VIN}"
                                       FontSize="12"
                                       TextColor="#999999"/>
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>

                <CollectionView.EmptyView>
                    <VerticalStackLayout HorizontalOptions="Center"
                                         VerticalOptions="Center"
                                         Spacing="15"
                                         Padding="40">
                        <Label Text="🚗"
                               FontSize="40"
                               HorizontalOptions="Center"/>
                        <Label Text="Транспорт не найден"
                               FontSize="16"
                               HorizontalOptions="Center"/>
                        <Button Text="Добавить транспорт"
                                Clicked="OnAddClicked"
                                BackgroundColor="#512BD4"
                                TextColor="White"
                                CornerRadius="10"
                                HeightRequest="45"/>
                    </VerticalStackLayout>
                </CollectionView.EmptyView>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>